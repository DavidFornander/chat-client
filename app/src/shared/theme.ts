import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#000F00',
            contrastText: '#FFFFFF'
        },
        secondary: {
            main: '#FF000F',
            contrastText: '#00FFF0'
        },
    }
})